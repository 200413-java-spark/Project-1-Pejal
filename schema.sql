CREATE TABLE transaction
(
transaction_date varchar,
product varchar,
price varchar,
payment_type varchar,
name varchar,
city varchar,
state varchar,
country varchar,
account_create varchar,
last_login varchar,
latitude varchar,
longitude varchar
);